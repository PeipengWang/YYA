# LightAI 苹果智慧果园管理系统 — 架构设计文档

---

## 1. 系统概览

系统面向山东沂源山地苹果种植，提供「传感器监测 → 智能决策 → 指令下发 → 日志闭环」的完整能力。整体由三个独立工程组成，通过 HTTP（含 SSE）契约协作：

| 工程                                    | 角色                 | 技术栈                                                                               | 端口                        |
| ------------------------------------- | ------------------ | --------------------------------------------------------------------------------- | ------------------------- |
| `LightAISoilMoist-IrrigationFrontend` | 前端（统一入口）           | Vue 3 + TypeScript + Vite + Element Plus + Pinia + vue-router + chart.js + marked | 3000（dev）/ Nginx 托管（prod） |
| `LightAISoilMoist-IrrigationBackend`  | 业务/设备后端            | Spring Boot 3.5 (Java 17) + JPA + H2 + Paho-MQTT + Knife4j                        | 8086                      |
| `YYA`（本仓库）                            | 智能决策后端（AI 智能体「小沂」） | FastAPI + LangChain(ReAct) + **DeepSeek（基座模型）** + DashScope + ChromaDB + SSE | 8001                      |

**核心分工一句话**：Spring 后端负责「真实世界的数据与执行」（设备接入、存储、指令下发、阈值/日志）；YYA 负责「认知与决策」（基于数据生成农事决策建议与对话）；前端把两者编排成统一界面。

---

## 2. 分层架构图

```
┌──────────────────────────────────────────────────────────────────────┐
│  浏览器 (Vue SPA)  LightAI 苹果智慧果园管理系统                         │
│  ├─ 实时监测 / 历史分析  ── 设备控制 / 阈值 / 决策日志                  │
│  └─ 智能决策管理页 (DecisionView) ── 浮动「小沂」AI 助手 (对话/语音)    │
└───────────────┬───────────────────────────┬──────────────────────────┘
                │  /api/* (除决策类)          │  /api/decision /api/chat
                │  → 8086                    │  /api/memory /api/asr → 8001
                ▼                            ▼
┌───────────────────────────────┐   ┌──────────────────────────────────┐
│  Spring Boot 业务后端 (8086)   │   │  YYA 智能体后端 (8001)            │
│  • 设备接入 OneNET + MQTT      │   │  • ReAct Agent + 6 个农事工具     │
│  • 实时缓存 /api/latest        │   │  • /api/chat/stream  (SSE 对话)   │
│  • /api/history/all (时序)     │   │  • /api/decision/summary (结构化) │
│  • /api/command (指令下发)     │   │  • /api/memory/* (记忆接口)       │
│  • /api/thresholds (阈值)     │   │  • RAG 知识库检索 + 水肥量化       │
│  • /api/decision-logs (日志)  │   │                                   │
│  • H2 持久化 + 决策日志落库    │   │   ── 反向调用 ──▶ /api/latest     │
│                               │   │                    /api/history/all│
└───────────────┬───────────────┘   └─────────────────┬────────────────┘
                │                                      │
                ▼                                      │
        ┌────────────────┐                            │
        │  OneNET 设备云   │  ◀── MQTT 订阅 ───────────┘ (YYA 取传感器数据)
        │  土壤传感器设备  │
        └────────────────┘
```

> **重要约束**：集成完全由**前端中介**。Spring 后端**不主动调用** YYA；YYA 在执行决策链路时，会**反向调用** Spring 后端的 8086 接口（`/api/latest`、`/api/history/all`）获取传感器数据。
>
> **模型基座**：YYA 的全部推理能力建立在 **DeepSeek `deepseek-chat`** 基座模型之上（temperature=0.3，由 `model/factory.py` 初始化）。LangChain ReAct、6 个农事工具、RAG 与结构化输出均围绕该基座构建——**DeepSeek 是 YYA 的「大脑」**。详见 §3.3.0。

---

## 3. 各组件职责

### 3.1 前端 `LightAISoilMoist-IrrigationFrontend`

- **框架**：Vue 3 + TypeScript + Vite，UI 用 Element Plus，状态用 Pinia（`stores/devices` 维护实时数据），路由用 vue-router（`LayoutView` 下挂 4 个页面）。
- **页面**（`src/router/index.ts`）：
  - `realtime` 实时数据监测
  - `history` 历史数据分析
  - `decision` **智能决策管理** ← 与 YYA 强相关
  - `thresholds` 阈值配置管理
- **API 层**（`src/api/index.ts`）：
  - 业务类（`/api` → 8086）：`fetchDevices`、`fetchLatest`、`fetchHistory`、`createSSEConnection`(实时推送)、`sendCommand`、`fetchCurrentThresholds`、`fetchDecisionLogs`。
  - **智能决策类（→ 8001）**：`chatStream`(`/api/chat/stream`)、`fetchASR`(`/api/asr`)、`fetchDecisionSummary`(`/api/decision/summary`)、`getTtsUrl`(直连 TTS :9000)。
- **关键页面 `DecisionView.vue`**：三栏卡片——①设备控制（水泵 F / 阈值 I,J,K,L 下发 → 8086）②当前生效阈值（→ 8086）③**小沂智能决策建议**（`fetchDecisionSummary('请分析当前果园状况，给出今日农田管理建议')` → 8001）。底部表格展示「决策日志」（指令下发记录，→ 8086）。右下角浮动按钮打开 AI 对话抽屉（流式对话 + 语音识别 + TTS 播报，均走 YYA/语音服务）。
- **代理配置**（`vite.config.ts`，dev 环境）：`/api/decision`、`/api/chat`、`/api/memory`、`/api/asr` → `http://localhost:8001`；其余 `/api`、`/api/decision-logs` → `http://localhost:8086`。

### 3.2 业务后端 `LightAISoilMoist-IrrigationBackend`（Spring Boot 8086）

- **设备接入**：`DeviceDataManager`（核心枢纽）通过 **Paho MQTT** 连接 OneNET 平台订阅设备实时上报，写入 `ConcurrentHashMap` 内存缓存（按设备+属性标识符 A~L 组织），SSE 推送线程读、MQTT 回调写，用读写锁保证并发安全。`OneNetApiClient` 负责 OneNET REST API 查询与指令下发，并在 MQTT 无数据时自动 API 兜底。
- **数据接口**（`DeviceController`）：
  - `GET /api/devices` 设备与属性元数据
  - `GET /api/latest` 最新数据（**YYA `soil_time_series_analysis` 正是调用此接口**）
  - `GET /api/history/all` 全属性历史（**YYA 同调此接口**）
  - `GET /api/stream` SSE 实时推送
  - `POST /api/command` 下发期望属性（水泵 F / 阈值 I,J,K,L），并写决策日志
- **阈值与物候期**（`ThresholdController` + `ThresholdService` + `PhenologyService`）：按物候期（休眠期→采收期等）与土壤类型（LOAM/SAND/…）管理属性阈值上下限，支持变更快照、人工覆盖物候期。
- **数据持久化**：H2 嵌入式数据库（`jdbc:h2:file:./data/threshold_db`），JPA 自动建表。
- **决策日志**：`DecisionLog` 实体 + `DecisionLogRepository`，记录水泵控制/阈值下发的执行结果与操作来源，供前端「决策日志」分页查询（`GET /api/decision-logs`）。
- **API 文档**：Knife4j（`/swagger-ui.html`）。

### 3.3 智能决策后端 `YYA`（FastAPI 8001，本仓库）

YYA 的本质是「以 **DeepSeek 为基座模型** 的 LangChain ReAct 智能体」。DeepSeek 是整个系统的**推理底座**——结构化决策（链路 A）与流式对话（链路 B）最终都由它生成文本与判断。

#### 3.3.0 模型基座：DeepSeek（推理底座）★

- **基座模型**：`deepseek-chat`（DeepSeek 通用对话模型）。
- **初始化**：`model/factory.py` 的 `ChatModelFactory.generator()` 通过
  `init_chat_model(model="deepseek-chat", model_provider="deepseek", temperature=0.3)` 创建。
- **双重作用**：
  1. 作为 ReAct Agent 的 `model` 参数，驱动 6 步工具编排与最终结论生成；
  2. 作为 `with_structured_output(DecisionSummaryOutput)` 的底层模型，将自然语言决策报告转为结构化 JSON（summary / advices / problematicSensors / alerts）。
- **为什么以 DeepSeek 为基座**：中文农事语境下推理与指令遵循能力较强，且 API 成本可控，适合 7×24 在线决策服务。
- **可配置**：`chat_model_name` 来自 `config/*.yml`，便于后续切换模型。

> **一句话**：DeepSeek `deepseek-chat` 是 YYA 的「大脑」；LangChain ReAct、6 个农事工具、RAG、结构化输出都是围绕这只大脑构建的「手脚与记忆」。

#### 3.3.1 YYA 内部层次（模型基座视角）

```
┌──────────────────────────────────────────────────────────┐
│  YYA 智能体后端 (FastAPI :8001)                            │
├──────────────────────────────────────────────────────────┤
│  接入层  api_server.py                                     │
│  • /api/chat/send  /api/chat/stream(SSE)                  │
│  • /api/decision/summary (结构化)  /api/memory/*           │
├──────────────────────────────────────────────────────────┤
│  智能体编排层  agent/react_agent.py (LangChain create_agent)│
│  • ReAct 循环 + 6 个农事工具（天气/土壤/RAG/物候/水肥/溯源） │
│  • decision_service.with_structured_output(DecisionSummary)│
├──────────────────────────────────────────────────────────┤
│  ▼ 模型基座层（推理底座）★                                  │
│  • 大模型：DeepSeek deepseek-chat  (temperature=0.3)       │
│  • 嵌入：DashScope text-embedding-v4                      │
│  • 向量库：ChromaDB（apple_farming_knowledge）             │
│  • 记忆：SqliteSaver（当前 checkpointer 未启用）          │
└──────────────────────────────────────────────────────────┘
        │
        └─ 反向调用 ─▶ Spring /api/latest + /api/history/all (8086)
```

- **接入层** `api_server.py`：`POST /api/chat/send`（一次性）、`POST /api/chat/stream`（SSE 流式）、`POST /api/decision/summary`（**结构化决策，前端智能决策页调用**）、`GET /api/memory/*`（记忆接口，基于 SqliteSaver）、静态托管。
- **智能体编排** `agent/react_agent.py`：`ReactAgent` 封装 LangChain `create_agent`（ReAct），`execute_stream` 仅输出不含工具调用的最终结论。
- **工具层** `agent/tools/`（6 个农事工具）：和风天气、土壤时序分析、RAG 检索、物候期判定、水肥量化、决策溯源。`soil_time_series_analysis` 调用 Spring 后端的 `/api/latest` 与 `/api/history/all`。
- **决策服务** `agent/decision_service.py`：`/api/decision/summary` 让 Agent 跑完完整 6 步链路后，用 `with_structured_output` 把自然语言报告转为结构化 JSON（summary / advices / problematicSensors / alerts）。
- **知识层** `rag/vector_store.py`：ChromaDB 向量库 + RAG 检索摘要（`load_document` 当前注释，知识库未自动加载）。
- **外部依赖**：DeepSeek API（推理）、DashScope（嵌入）、和风天气 API（气象）、土壤设备 API（Spring 8086）、TTS(:9000)、ASR(:9001)。

---

## 4. 「智能决策」集成链路（重点）

「智能决策」是系统中唯一由 YYA 提供的功能块，存在**两条调用链**：

### 链路 A：结构化决策建议（决策管理页）

```
前端 DecisionView
  │  POST /api/decision/summary  {prompt:"请分析当前果园状况，给出今日农田管理建议"}
  ▼  (vite 代理 → 8001)
YYA  api_server.py → decision_service.generate_decision_summary(prompt, agent)
  │  agent.execute_stream(prompt) 触发 ReAct 6 步链路：
  │    1) soil_time_series_analysis ──▶ Spring /api/latest + /api/history/all (8086)
  │    2) get_yiyuan_weather_forecast ─▶ 和风天气
  │    3) judge_apple_phenological_period
  │    4) rag_apple_knowledge_search ─▶ ChromaDB
  │    5) calculate_water_fertilizer_amount
  │    6) decision_trace_explain
  │  → 自然语言决策报告
  ▼
decision_service 用 with_structured_output(DecisionSummaryOutput) 转换
  ▼
{ code:0, data:{ summary, advices[], problematicSensors[], alerts[] } }
  ▼
前端渲染：综合摘要 / 分项建议卡片 / 异常传感器 / 告警
```

> 注意：决策的数据底座来自 Spring 后端（传感器实时/历史值），YYA 自身不直连硬件，而是经 8086 间接获取。

### 链路 B：AI 对话助手（浮动「小沂」抽屉）

```
前端 chatStream() → POST /api/chat/stream (SSE) → YYA execute_stream
  YYA 内部同样可调用 6 个工具（含反向取 8086 传感器数据）
  SSE 增量返回 chunk；结束返回 tts_text
前端 playTTS(tts_text) → 直连 TTS :9000 播报
前端录音 → fetchASR(blob) → POST /api/asr (→ ASR :9001) → 识别文本回填
```

### 决策日志 vs 智能决策（易混淆点）

- **智能决策建议**（链路 A 产出）：YYA 生成的农事建议 JSON，由前端实时展示，**不落 Spring 库**。
- **决策日志**（`/api/decision-logs`）：Spring 后端记录的**指令下发执行结果**（水泵开关、阈值设置），与 YYA 建议是「建议 → 人工/系统执行 → 日志」的上下游关系。

---

## 5. 接口契约（前端视角）

| 前端调用                                                      | 目标                       | 说明                            |
| --------------------------------------------------------- | ------------------------ | ----------------------------- |
| `/api/devices`,`/api/latest`,`/api/history`,`/api/stream` | 8086                     | 设备元数据/实时/历史/SSE               |
| `/api/command`                                            | 8086                     | 水泵/阈值指令下发                     |
| `/api/thresholds/*`                                       | 8086                     | 物候期阈值配置与查询                    |
| `/api/decision-logs`                                      | 8086                     | 指令下发决策日志                      |
| `/api/decision/summary`                                   | **8001**                 | **结构化智能决策（YYA）**              |
| `/api/chat/stream`                                        | **8001**                 | **流式对话（YYA）**                 |
| `/api/memory/*`                                           | **8001**                 | 记忆接口（YYA，当前 checkpointer 未启用） |
| `/api/asr`                                                | **8001**(代理) → ASR :9001 | 语音识别                          |
| TTS `:9000/tts-stream`                                    | 直连                       | 语音播报                          |

---

## 6. 部署拓扑（参考 `nginx.conf` + `vite.config.ts`）

- **生产**：Nginx 在 9533(HTTPS) 托管前端静态产物并反向代理 `/api/` → 8001（YYA），同时 9534(HTTPS) 代理已弃用的音乐服务；Spring 8086 与 YYA 8001、TTS 9000、ASR 9001 作为后端服务独立运行。
- **开发**：前端 `npm run dev`(3000) 用 vite 代理分流到 8086/8001；YYA `python api_server.py`(8001)；Spring `mvn spring-boot:run`(8086)。

---

