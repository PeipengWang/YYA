# 决策分析模式结构化输出改造方案

## 一、问题诊断

### 1.1 当前架构

```
POST /api/decision/summary
  → decision_service.generate_decision_summary(prompt, agent)
    → agent.execute_stream(prompt)          # ReAct Agent 7步链路
      → soil_time_series_analysis           # 工具1：土壤时序
      → get_yiyuan_weather_forecast         # 工具2：天气
      → judge_apple_phenological_period     # 工具3：物候期
      → rag_apple_knowledge_search          # 工具4：RAG知识库
      → calculate_water_fertilizer_amount   # 工具5：水肥计算
      → decision_trace_explain              # 工具6：决策溯源
      → generate_decision_summary           # 工具7：结构化JSON ⚠️
        → structured_chat_model.invoke()    # ⚠️ 嵌套LLM调用
    → 从Agent最终输出中正则提取JSON          # ⚠️ 解析脆弱
    → validate_decision_response()          # 逐字段校验
    → {"code": 0, "data": {...}}
```

### 1.2 核心痛点

| 痛点 | 位置 | 说明 |
|------|------|------|
| **嵌套LLM调用** | `agri_tools.py:270` | `generate_decision_summary` 工具内部调用 `structured_chat_model.invoke()`，该调用在Agent的消息上下文之外，无法感知完整对话历史 |
| **上下文传递不可靠** | `agri_tools.py:248` | 工具入参 `context: str` 由Agent模型自行组装，可能遗漏关键数据、格式不完整、或被Agent简化 |
| **JSON被污染** | `react_agent.py:60-63` | `execute_stream` 只过滤 `tool_calls` 为空的消息，工具返回的JSON字符串作为 ToolMessage 输入后，Agent模型会再次推理并输出最终消息，可能给JSON包裹markdown代码块、添加额外文字说明 |
| **正则提取脆弱** | `decision_service.py:129-131` | 依赖 `re.search(r"```(?:json)?\s*([\s\S]*?)```", raw_text)` 提取JSON，模型不按预期输出时直接解析失败 |
| **两阶段校验冗余** | Pydantic + 手动校验 | `with_structured_output` 已做schema约束，`decision_service.py` 又逐字段手动校验，逻辑重复 |

### 1.3 问题根因链

```
Agent模型组装context不完整
  → generate_decision_summary收到的上下文信息缺失
    → structured_chat_model生成的JSON字段不准确/不完整
      → Agent模型最终输出时可能添加额外文字
        → decision_service正则提取失败 或 校验不通过
          → 返回 {"code": -1, "message": "..."}
```

---

## 二、方案评估

### 方案A：`create_agent(response_format=ToolStrategy(Model))` （用户提议方案）

**原理**：ToolStrategy 将 Pydantic schema 转换为一个"必须调用"的虚拟工具，Agent完成数据采集后必须调用该工具，工具的入参即为结构化输出。

```python
from langchain.agents import create_agent
from langchain.agents.structured_output import ToolStrategy

decision_agent = create_agent(
    model=chat_model,
    system_prompt=PROMPT_DECISION_MODE,
    tools=[...6个数据采集工具...],        # 不含 generate_decision_summary
    response_format=ToolStrategy(DecisionSummaryOutput),  # 框架层约束
)
```

**优点**：

1. **框架级约束**：结构化输出由 LangChain 框架在Agent层面保证，不需要手动在工具内嵌套LLM调用
2. **消除嵌套调用**：不再需要 `generate_decision_summary` 工具，减少一次LLM往返
3. **输出即数据**：Agent最终输出直接是 `DecisionSummaryOutput` 实例，无需正则提取和二次解析
4. **Schema统一**：Pydantic模型一处定义，无需 `decision_service.py` 手动逐字段校验
5. **语义清晰**：决策Agent和咨询Agent职责分离，各自优化

**缺点**：

1. **DeepSeek兼容性风险（关键）**：`ToolStrategy` 底层依赖 OpenAI 风格的 `tool_choice: "required"` 机制，DeepSeek对该特性的支持程度需要实测验证。如果DeepSeek不完全支持强制工具调用，Agent可能不会调用结构化输出工具
2. **大规模数据透传问题**：ToolStrategy 要求LLM在一次工具调用中填写 `DecisionSummaryOutput` 的全部字段（含嵌套的 `advices`/`problematicSensors`/`alerts` 数组），这对模型的指令遵循能力要求极高，可能出现字段遗漏或幻觉填充
3. **ReAct链兼容性**：当前Agent的7步链路有严格顺序要求，ToolStrategy 添加的虚拟工具可能被Agent在错误时机调用（如在数据采集完成前就尝试输出结构化结果）
4. **单Agent变双Agent**：需要维护两个Agent实例（决策专用 + 咨询通用），增加内存占用和配置复杂度
5. **框架版本依赖**：`response_format` + `ToolStrategy` 是 LangChain 较新特性，API稳定性不如核心功能
6. **调试困难**：ToolStrategy 的行为对开发者不透明，出问题时难以定位是模型问题还是框架问题

**适用性判断**：方案A适合对OpenAI模型且有强结构化需求、不介意双Agent架构的场景。当前项目使用DeepSeek且已有可用的 `with_structured_output` 方案，风险收益比不够理想。

---

### 方案B：后处理式结构化输出（推荐）

**原理**：Agent在决策模式下输出完整的自然语言分析报告（去掉 `generate_decision_summary` 工具），在 `decision_service.py` 中将报告作为输入，调用 `structured_chat_model.invoke()` 转换为结构化JSON。

```
Agent 6步链路（不含 generate_decision_summary）
  → 自然语言决策报告
    → decision_service.py 调用 structured_chat_model.invoke(报告)
      → DecisionSummaryOutput Pydantic实例
        → {"code": 0, "data": {...}}
```

**优点**：

1. **零Agent改动**：Agent保持与咨询模式相同的工具集（去掉专用工具），系统提示词无需模式分支
2. **结构化输出可靠**：`chat_model.with_structured_output(DecisionSummaryOutput)` 已在当前代码中验证可用（`agri_tools.py:242`），直接复用
3. **职责分离清晰**：Agent负责数据采集和推理（它擅长的），`decision_service` 负责格式转换（框架擅长的）
4. **Agent输出不丢数据**：自然语言报告比 `context` 字符串更完整，因为Agent会自然地组织所有发现
5. **DeepSeek友好**：不依赖 `tool_choice` 等OpenAI特有参数
6. **易于调试**：每个环节的输入输出都可独立检查

**缺点**：

1. **多一次LLM调用**：Agent推理 + 结构化转换 = 两次LLM调用（但当前方案也是两次：Agent推理 + `generate_decision_summary` 内部调用）
2. **报告可能遗漏数据**：Agent自然语言报告可能没有覆盖所有传感器异常值（可通过优化提示词解决）
3. **延迟略增**：串行两次调用增加约1-3秒延迟

**实现复杂度**：低。核心改动仅在 `decision_service.py` 和 `main_prompt.txt`。

---

### 方案C：直接顺序调用 + 结构化输出（跳过Agent）

**原理**：决策模式下完全不走ReAct Agent，在 `decision_service.py` 中顺序调用6个工具，将结果拼成上下文，直接调用 `structured_chat_model.invoke()` 生成结构化JSON。

```python
def generate_decision_summary(prompt, agent):
    # 1. 顺序调用工具
    soil_data = soil_time_series_analysis.invoke({"device_name": "device", "time_range": 7})
    weather_data = get_yiyuan_weather_forecast.invoke({"location": "沂源县"})
    phenology_data = judge_apple_phenological_period.invoke({})
    rag_data = rag_apple_knowledge_search.invoke({"query": "当前果园日常管理"})
    water_fertilizer_data = calculate_water_fertilizer_amount.invoke({"analysis_context": ...})
    trace_data = decision_trace_explain.invoke({"decision_context": ...})
    
    # 2. 拼接上下文
    context = f"""..."""
    
    # 3. 一次调用生成结构化输出
    result = structured_chat_model.invoke(context)
    return {"code": 0, "data": result.model_dump()}
```

**优点**：

1. **最可靠**：完全消除LLM不确定性，工具调用顺序和参数由代码控制
2. **延迟最低**：无Agent思考循环，工具调用并行化后可进一步优化
3. **无模式耦合**：决策模式完全独立于咨询模式

**缺点**：

1. **失去Agent推理能力**：无法处理工具调用失败、数据异常等需要动态决策的场景
2. **硬编码脆弱**：工具链顺序固化，业务逻辑变更需改代码
3. **提示词工程迁移**：`main_prompt.txt` 中的决策逻辑需重新实现为代码逻辑
4. **工具调用参数需手动构建**：如 `rag_apple_knowledge_search(query)` 的 query 不再由Agent动态生成
5. **完全绕过Agent框架**：丧失了中间件（监控、日志）的自动覆盖

**适用性判断**：如果决策模式的需求高度固定，且对可靠性要求极高（如生产环境），可以考虑。但一旦物候期规则、RAG逻辑变化，需同步改代码。

---

### 方案D：优化当前方案（最小改动）

**原理**：保持当前架构不变，针对性修复各环节的问题。

**具体优化点**：

1. **改进context传递**：不依赖Agent组装context，在 `generate_decision_summary` 工具内部获取Agent的消息历史，自行提取前面工具的输出结果
2. **防止JSON污染**：在提示词中强化 "输出必须是纯JSON，不含任何额外文字" 的约束，并在 `execute_stream` 中判断如果检测到JSON开头则直接返回
3. **增加重试机制**：JSON解析失败时自动重试，`decision_service.py` 最多重试2次
4. **合并校验逻辑**：去掉 `decision_service.py` 的手动校验，完全信任 `with_structured_output` 的Pydantic约束

**优点**：

1. **改动最小**：仅修改 `agri_tools.py` 和 `decision_service.py`
2. **风险最低**：不改变架构，不引入新概念
3. **快速上线**：1-2小时可完成

**缺点**：

1. **治标不治本**：架构层面的问题（嵌套LLM调用、JSON串行传递）依然存在
2. **维护成本累积**：后续问题可能反复出现

---

## 三、方案对比总结

| 维度 | 方案A ToolStrategy | **方案B 后处理** | 方案C 跳过Agent | 方案D 优化现状 |
|------|:--:|:--:|:--:|:--:|
| 可靠性 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| DeepSeek兼容 | ⚠️ 需验证 | ✅ 已验证 | ✅ 已验证 | ✅ 已验证 |
| 改动量 | 大 | **中** | 大 | 小 |
| Agent推理保留 | ✅ | ✅ | ❌ | ✅ |
| 消除嵌套LLM | ✅ | ❌（但同级） | ✅ | ❌ |
| 消除JSON解析 | ✅ | ✅ | ✅ | ❌ |
| 维护复杂度 | 高 | **低** | 中 | 中 |
| 可调试性 | 差 | **好** | 好 | 中 |
| 额外延迟 | 无 | ~1-3s | 无 | 无 |

---

## 四、推荐方案：方案B（后处理式结构化输出）

### 4.1 推荐理由

1. **已验证的技术路径**：`with_structured_output(DecisionSummaryOutput)` 在 `agri_tools.py:242` 已正常工作，方案B只是改变其调用位置（从工具内移到工具外）
2. **DeepSeek零风险**：不依赖任何OpenAI特有参数
3. **架构更清晰**：Agent做它擅长的事（推理），格式化由框架保证
4. **改动可控**：约4个文件，200行变更，不影响咨询模式
5. **可回滚**：改动集中在 `decision_service.py`，出问题可快速回退

### 4.2 实现计划

#### 第1步：精简Agent工具集和提示词

**文件**：`agent/react_agent.py`、`prompts/main_prompt.txt`

- 从Agent工具列表移除 `generate_decision_summary`
- `main_prompt.txt` 删除"模式一"和"模式二"的区分，统一为一条工具链：
  1. soil_time_series_analysis
  2. get_yiyuan_weather_forecast
  3. judge_apple_phenological_period
  4. rag_apple_knowledge_search
  5. calculate_water_fertilizer_amount
  6. decision_trace_explain
- 输出规则：统一输出完整的自然语言决策报告（含土壤分析、天气、物候、水肥方案、溯源）

#### 第2步：重写 `decision_service.py`

**文件**：`agent/decision_service.py`

```python
def generate_decision_summary(prompt: str, agent) -> dict:
    # 1. Agent生成自然语言报告
    full_response = ""
    for chunk in agent.execute_stream(prompt):
        full_response += chunk
    full_response = full_response.strip()
    
    if not full_response:
        return {"code": -1, "message": "智能体分析返回为空"}
    
    # 2. 用结构化模型转换
    system_prompt = """你是沂源苹果智能农事决策助手。根据以下决策报告，生成结构化JSON。
严格遵循规则：
1. advices：仅保留有实际建议的分类
2. problematicSensors：只列出异常传感器，value为纯数字
3. alerts：最多5条，按优先级排序
4. 所有字段必须存在，无数据为空数组"""
    
    full_context = f"{system_prompt}\n\n决策报告：\n{full_response}"
    
    try:
        result: DecisionSummaryOutput = structured_chat_model.invoke(full_context)
        return {"code": 0, "data": result.model_dump()}
    except Exception as e:
        logger.exception(f"结构化输出失败: {e}")
        return {"code": -1, "message": f"结构化输出失败: {str(e)}"}
```

Pydantic模型和 `structured_chat_model` 从 `agri_tools.py` 移至 `decision_service.py`。

#### 第3步：清理 `agri_tools.py`

- 删除 `generate_decision_summary` 工具函数
- 删除 `AdviceItem`/`ProblematicSensor`/`AlertItem`/`DecisionSummaryOutput` Pydantic模型
- 删除 `structured_chat_model` 全局变量

#### 第4步：更新 `__init__.py` 和兼容层

**文件**：`agent/tools/__init__.py`、`agent/tools/agent_tools.py`

- 从 `farming_tool_list` 和导出列表移除 `generate_decision_summary`

#### 第5步：测试验证

1. 单元测试：`validate_decision_response` 覆盖正常/异常/边界case
2. 集成测试：调 `/api/decision/summary` 验证完整链路
3. 回归测试：调 `/api/chat/send` 验证咨询模式不受影响

### 4.3 风险与缓解

| 风险 | 缓解措施 |
|------|----------|
| Agent报告遗漏传感器异常信息 | 在提示词中明确要求："报告中必须逐项列出所有传感器状态，标注异常指标" |
| `with_structured_output` 处理大段文本时性能下降 | 报告通常在2000字以内，在DeepSeek上下文窗口内 |
| 两段LLM调用增加延迟 | 均为流式调用，总延迟增加约1-3s，在可接受范围 |
| 结构化转换丢失细节 | 保留原始报告在日志中，出问题时可通过日志复盘 |

---

## 五、方案A补充说明

如果后续想尝试 `ToolStrategy` 方案，建议按以下步骤验证：

1. **最小可行性验证**：单独写一个测试脚本，用 DeepSeek 模型 + `create_agent(response_format=ToolStrategy(SimpleModel))` 测试基本功能
2. **确认兼容性**：检查 DeepSeek API 是否返回 `tool_choice` 相关错误
3. **渐进式迁移**：先在非核心场景验证，确认稳定后再替换主流程

当前阶段推荐方案B作为首选，因为它在可靠性、改动量和风险之间取得了最佳平衡。

---

## 六、无法改造的边界情况

以下场景下任一方案可能都无法完美解决，需要降级处理：

1. **DeepSeek模型输出乱码**：偶发的JSON格式错误 → 增加重试（最多2次）+ 降级为自然语言返回
2. **传感器全部离线**：无数据可分析 → 返回空结构 + `alerts` 中包含"传感器离线"告警
3. **决策页面接口规范变更**：前端字段调整 → 只需更新Pydantic模型，无需改Agent逻辑
